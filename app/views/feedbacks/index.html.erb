<h1>Listing feedbacks</h1>

<table>
  <!-- <tr>
    <th>Asunto</th>
    <th>Contenido</th>
    <th>Feedback type</th>
    <th>Project</th>
    <th>Comentario</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>-->



  <% @feedbacks.each do |feedback| %>
      <tr>
        <div class="seccion2">
          <div>
            <%= image_tag(   FeedbackType.find(feedback.feedback_type_id).name + ".png", :alt => "Moove-It",:class => "ImgFeedback") %>
            <a href="feedbacks/<%= feedback.id %>" class="fill-div">
              <h5 class="fechaFeedback" >03/09/2012</h5>
              <h4 class="show_Asunto"><%= feedback.asunto %></h4>
              <h6 >Ultimo comentario echo por mauro@mooveit , 06 de agosto 2012</h6>
            </a>
          </div>
        </div>

        <!--  <td><%= feedback.asunto %></td>
    <td><%= feedback.contenido %></td>
    <td><%= feedback.feedback_type_id %></td>
    <td><%= feedback.project_id %></td>
    <td><%= feedback.comentario_id %></td>
    <td><%= link_to 'Show', feedback %></td>
    <td><%= link_to 'Edit', edit_feedback_path(feedback) %></td>
    <td><%= link_to 'Destroy', feedback, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
  <% end %>
</table>

<br />

<%= link_to 'New Feedback', new_feedback_path %>